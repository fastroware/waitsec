# UI Copy Compatibility Pointer

This path remains available so older links do not break.

For UI copy, labels, helper text, accessibility text, tooltips, and empty states, read [`../../waitsec-ui/references/ui-copy.md`](../../waitsec-ui/references/ui-copy.md).

Use `waitsec-ui` as the source of truth for user-facing copy guidance.
